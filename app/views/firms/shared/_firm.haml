.entity
  .content
    %h2.title
      = link_to item.name, item
    - unless item.description.nil?
      %p
        = item.description[0..300]

        - if item.description.length > 300
          %span ...
          = link_to 'еще', item

    - unless item.address.nil?
      %p
        %strong Адрес:
        = item.address

    %h3
      Спонисируемые специальности
    = render partial: 'shared/tags', locals:{tags: item.specialties}